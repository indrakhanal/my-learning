import express from "express"; import cors from "cors"; import path from "path"; import { authRouter } from "./routes/auth.js"; import { notesRouter } from "./routes/notes.js"; import { tagsRouter } from "./routes/tags.js"; import { uploadsRouter } from "./routes/uploads.js";
export const app = express(); app.use(cors({ origin: process.env.WEB_ORIGIN, methods: ["GET", "POST", "PUT", "DELETE"] })); app.use(express.json({ limit: "1mb" })); app.use("/uploads", express.static(path.resolve(process.cwd(), "uploads"))); app.get("/health", (_q, s) => s.json({ status: "ok" })); app.use("/api/auth", authRouter); app.use("/api/notes", notesRouter); app.use("/api/tags", tagsRouter); app.use("/api/uploads", uploadsRouter); app.use((err: any, _q: any, res: any, _n: any) => { if (err?.name === "ZodError") return res.status(400).json({ error: "Validation failed", details: err.issues }); if (err?.code === "P2002") return res.status(409).json({ error: "A record with that value already exists" }); console.error(err); return res.status(500).json({ error: "Internal server error" }); });
